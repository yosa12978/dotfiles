#!/bin/sh

alias vim="nvim"
alias codium="codium --ozone-platform-hint=auto"
alias code="codium --ozone-platform-hint=auto"
alias fzf="fzf --preview 'bat --style=numbers --color=always {}'"
alias glow="glow -t"

alias wu="wg-quick up wg0"
alias wd="wg-quick down wg0"

alias http="http --style=pie-dark"
alias https="https --style=pie-dark"

alias discord="discord --no-sandbox"

alias terraform="tofu"

tm() {
	tmux new -s "${PWD##*/}" || tmux attach -t "${PWD##*/}"
}
